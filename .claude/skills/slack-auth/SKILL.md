---
name: slack-auth
description: servers/slack-auth/server.js を起動する。Slack OAuth認証サーバーを立ち上げたいときに使う。
disable-model-invocation: true
allowed-tools: Bash
---

`servers/slack-auth/server.js` を起動する。

```bash
node /Users/gogo/gogo/servers/slack-auth/server.js
```

上記コマンドをBashで実行してサーバーを起動する。
起動後、ポート3000でリッスンしていることを確認して報告する。
